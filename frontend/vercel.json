{
  "installCommand": "npm install --prefix ../packages/shared --include=dev --no-audit --no-fund && npm install --prefix ../packages/tokens --include=dev --no-audit --no-fund && npm install",
  "buildCommand": "npm run build"
}
