{
  "installCommand": "npm install --prefix ../packages/shared --no-audit --no-fund && npm install --prefix ../packages/tokens --no-audit --no-fund && npm install",
  "buildCommand": "npm run build"
}
