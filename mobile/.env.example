# Copy to .env and set your computer's LAN IP for local physical-device testing,
# or your Render URL for testing against the deployed API.
# Find your LAN IP: ipconfig getifaddr en0  (macOS)
# EXPO_PUBLIC_API_URL=http://YOUR_LAN_IP:8000
EXPO_PUBLIC_API_URL=https://eljiran-api.onrender.com
